# OptiWealth consolidated dependency reference
#
# This file primarily lists Python analytics microservice dependencies.
# The Java backend uses Maven (see backend-springboot/pom.xml).
# The React frontend uses npm (see frontend-react/package.json).
#
# To install these Python dependencies:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Pin versions later after validation (e.g., with `pip freeze > requirements.lock`).
# For now, keep them flexible to ease upgrades.

# --- Python Analytics Microservice ---
Flask>=3.0.0
APScheduler>=3.10.0
yfinance>=0.2.40
pandas>=2.2.0
numpy>=1.26.0
statsmodels>=0.14.0
arch>=6.3.0
python-dotenv>=1.0.0
google-genai>=0.1.0

# Optional (future enhancements)
# scikit-learn>=1.5.0        # For ML-based forecasting models
# redis>=5.0.0               # For caching market data
# gunicorn>=22.0.0           # For production WSGI serving
# requests>=2.32.0           # Explicit HTTP client if needed

# --- Notes ---
# Java backend build: `./mvnw clean install`
# Frontend setup: `npm install`
# Consider creating per-service lock files:
#   - requirements.lock (pip freeze)
#   - Maven dependency plugin report
#   - package-lock.json (already generated by npm)

